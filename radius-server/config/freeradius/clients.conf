# FreeRADIUS Clients Configuration
# Defines which devices (NAS/routers) can communicate with the RADIUS server
#
# For Teltonika routers, you'll add each router here with its shared secret

# ===========================================
# Default client - Teltonika RUT200 Router
# CRITICAL: This shared secret MUST match the router's chilli.radiussecret
# Router config: uci show chilli | grep radiussecret
# ===========================================
client default {
    ipaddr = 0.0.0.0/0
    secret = lPvk2g6aQuMWpmAGnQrwQ
    shortname = default
    nastype = other
    # Limit to specific virtual server
    virtual_server = default
}

# ===========================================
# Example: Single Teltonika Router
# ===========================================
# client router_001 {
#     ipaddr = 192.168.1.100
#     secret = your-secure-secret-here
#     shortname = Router-001
#     nastype = other
#     # Link to RouterLogger router_id
#     # nas_type = teltonika
# }

# ===========================================
# Example: Subnet of routers
# Use when routers have dynamic IPs
# ===========================================
# client teltonika_network {
#     ipaddr = 10.0.0.0/8
#     secret = shared-network-secret
#     shortname = teltonika-net
#     nastype = other
# }

# ===========================================
# Dynamic Clients from SQL
# Loads clients from the 'nas' table
# ===========================================
# Uncomment to enable dynamic clients
# client dynamic {
#     ipaddr = 0.0.0.0/0
#     dynamic = yes
#     lifetime = 3600
# }

