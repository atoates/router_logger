# RouterLogger RADIUS Server Environment Variables
# Copy this file to .env and fill in your values
# Command: cp env.example .env

# ===========================================
# Database Configuration (MariaDB for RADIUS)
# ===========================================
RADIUS_DB_ROOT_PASSWORD=change-this-secure-root-password
RADIUS_DB_PASSWORD=change-this-secure-radius-password

# ===========================================
# RADIUS Configuration
# ===========================================
# Shared secret for router authentication
# Must match the secret configured on each Teltonika router
RADIUS_SECRET=change-this-shared-secret

# ===========================================
# RouterLogger Integration
# ===========================================
# Your RouterLogger backend URL (Railway deployment)
ROUTERLOGGER_API_URL=https://your-backend.up.railway.app

# ===========================================
# Captive Portal Configuration
# ===========================================
# Session encryption secret (generate with: openssl rand -hex 16)
SESSION_SECRET=change-this-session-secret

# UAM secret for CoovaChilli/Teltonika authentication
# Must match the UAM secret configured on the Teltonika router
UAM_SECRET=change-this-uam-secret

# Free tier limits
FREE_SESSION_DURATION=86400  # 24 hours in seconds
FREE_DATA_LIMIT_MB=500       # MB per session
FREE_COOLDOWN_HOURS=3        # Hours before same device can reconnect

# Company name shown on portal
COMPANY_NAME=Your Company Name

# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# ===========================================
# SMS Verification (Optional - Twilio)
# ===========================================
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ===========================================
# Production Settings
# ===========================================
NODE_ENV=production
TZ=Europe/London

